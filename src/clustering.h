#ifndef INCLUDE_clustering_h_
#define INCLUDE_clustering_h_

#include "base.h"

int find_nearest(NumericVector, model**, int, int);
double radius(int, int, NumericVector);
double learning_rate(int, int, NumericVector);

#endif //INCLUDE_clustering_h_
